{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "for f in runtime-parts/part*.b64; do base64 -d \"$f\"; done | tar -xzf - && python deploy-patch.py && npm install && npm run build:api"
  },
  "deploy": {
    "startCommand": "API_PORT=$PORT npm run start -w @fsm/api",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
